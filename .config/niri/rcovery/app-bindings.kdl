binds {
  Mod+V { spawn-sh "dms ipc call clipboard toggle"; }
  Mod+Space { spawn-sh "dms ipc call spotlight toggle"; }
  // Mod+V { spawn-sh "qs -c noctalia-shell ipc call launcher clipboard"; }
  // Mod+Space { spawn-sh "qs -c noctalia-shell ipc call launcher toggle"; }

  Mod+Return { spawn "ghostty"; }
  Mod+E { spawn-sh "nautilus --new-window"; }
  // Mod+E { spawn-sh "ghostty -e yazi"; }
  Mod+B { spawn "brave"; }
  Mod+O { spawn-sh "obsidian -disable-gpu --enable-wayland-ime"; }
  Mod+A { spawn-sh "brave --app=https://claude.ai/new"; }
  Mod+Shift+A { spawn-sh "brave --app=https://grok.com"; }
  Mod+Y { spawn-sh "brave --app=https://youtube.com"; }
  Mod+M { spawn-sh "brave --app=https://music.youtube.com"; }
  Mod+G { spawn-sh "brave --app=https://web.whatsapp.com"; }
  Mod+Alt+G { spawn-sh "brave --app=https://messages.google.com/web/conversations"; }

  Mod+T { spawn-sh "ghostty -e dgop"; }
  Mod+D { spawn-sh "ghostty -e lazydocker"; }

  Ctrl+Mod+Backspace { spawn-sh "dms ipc call lock lock"; }
  // Ctrl+Super+Space { spawn-sh "dms ipc call wallpaper next; dms ipc call theme dark"; }
  // Ctrl+Mod+Backspace { spawn-sh "qs -c noctalia-shell ipc call lockScreen lock"; }
  // Ctrl+Super+Space { spawn-sh "omarchy-theme-bg-next"; }

  Print { screenshot; }
  Ctrl+Print { screenshot-screen; }
  Alt+Print { screenshot-window; }
}
